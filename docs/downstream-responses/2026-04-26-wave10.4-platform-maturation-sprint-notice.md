# Wave 10.4 Platform Maturation Sprint — Delivery Notice

**Date:** 2026-04-26
**Functional HEAD:** `f5e3cff` (Wave 10.4 W4-G integration fix)
**Notice HEAD:** `f5e3cff`
**T3 evidence:** PENDING — W4-G gate run requires `VOLCE_API_KEY`; all prior T1/T2 evidence clean
**Gate pending:** true (T3 gate requires user action — see W4-G instructions below)

---

## 1. Purpose

Wave 10.4 delivers a **+5 readiness-point increment** (75.5 → ~80.5 conservative band) through 6 parallel tracks targeting the highest-leverage dimension gaps identified in the Wave 10.3 downstream re-review.

Four Wave 11 items were promoted into scope to reach the +5 target in one wave:
- **P1-4 EvolutionExperiment + ExperimentStore** (largest headroom: 可进化能力 weight 8)
- **P1-2 ExtensionManifest unification** (can扩展能力 weight 9)
- **P1-3 Recovery state machine** (长程任务稳定执行 weight 12 — largest weight)
- **KG SQLite backend** (可扩展能力 + 安全 dimensions)

---

## 2. Closures by Problem Class

| Class | Description | Track | Tests Added |
|---|---|---|---|
| **F** | Spine gaps in evolve dataclasses (RunPostmortem, CalibrationSignal, ProjectPostmortem, EvolveResult, EvolveMetrics, EpisodeRecord) | W4-A | 9 tests |
| **Q** | EvolutionExperiment dataclass + ExperimentStore (InMemory+Sqlite) + EvolveEngine wired | W4-A | 15 tests |
| **P** | ExtensionManifest Protocol + ExtensionRegistry + unified /manifest + `hi-agent extensions` CLI | W4-B | 22 tests |
| **L** | Process recovery rehydration on lifespan startup + RunQueue stale-lease expiry | W4-C | 4 tests + 1 skip |
| **H** | Cross-tenant route audit (routes_tools_mcp.py 4 handlers fixed; routes_knowledge.py confirmed) + 5 cross-tenant denial test files | W4-D | 25 tests |
| **G** | Rule 6 inline-fallback sweep: 8 `x or DefaultX()` sites converted to required kwargs; check_rules.py regex extended | W4-D | 19 tests |
| **K** | RunExecutionContext through 5 durable writers (IdempotencyStore, EventStore, TeamRunRegistry, SessionStore, ArtifactRegistry) | W4-E | 13 tests |
| **J** | RunResult.to_dict() optional spine keys (back-compat); TaskContract optional body spine (precedence over middleware) | W4-E | 6 tests |
| **R** | SqliteKnowledgeGraphBackend (tenant-scoped, Protocol-compliant); posture-aware factory (JSON=dev default, Sqlite=opt-in) | W4-F | 21 tests |
| **I** | Rule 7 alarms: release_gate episode skip (2 sites) + failover Retry-After parse + mcp/transport stderr tail | W4-F | 8 tests |

**Total new tests: 142** (unit + integration)
**Full suite: 4,515 passing** (4 pre-existing failures, all pre-Wave 10.4)

---

## 3. Readiness Delta (Downstream §5 Scorecard)

Baseline: Wave 10.3 post-W3-F = **~75.5** (T3 still pending; see Wave 10.3 notice).

| Dimension | Weight | W10.3 | W10.4 Expected | Gain | Driver |
|---|---|---|---|---|---|
| 长程任务稳定执行 | 12 | 9.0 | 10.5 | **+1.5** | W4-C: _rehydrate_runs + expire_stale_leases + cross-loop test |
| 安全与租户隔离 | 10 | 8.4 | 9.0 | **+0.6** | W4-D: 5 routes cross-tenant + 4 handler fixes |
| 架构完整性 | 9 | 7.7 | 8.4 | **+0.7** | W4-D Rule 6 sweep (8 sites) + W4-A evolve spine |
| 可扩展能力 | 9 | 6.8 | 8.3 | **+1.5** | W4-B ExtensionManifest + W4-F KG SQLite |
| 可使用能力 | 8 | 6.2 | 6.7 | **+0.5** | W4-B `hi-agent extensions list/inspect` + docs |
| 状态一致性与可恢复 | 8 | 6.2 | 7.0 | **+0.8** | W4-C rehydration + W4-E 5-writer spine consistency |
| 证据与 artifact 能力 | 8 | 6.1 | 7.0 | **+0.9** | W4-E Artifact spine + ArtifactRegistry exec_ctx |
| 可进化能力 | 8 | 3.9 | 6.0 | **+2.1** | W4-A EvolutionExperiment + ExperimentStore + all evolve dataclasses |
| 测试与发布门禁 | 8 | 7.6 | 7.9 | **+0.3** | verify_clean_env.py 254-test bundle (W4-G) |
| 团队/多智能体工作流 | 8 | 5.0 | 6.5 | **+1.5** | W4-E TeamRun spine + TeamRunRegistry exec_ctx |
| 可观测与诊断 | 7 | 7.7 | 7.9 | **+0.2** | W4-F 4 Rule 7 alarms |
| 架构宣称功能可用能力 | 5 | 8.0 | 8.4 | **+0.4** | W4-B unified /manifest + W4-E HTTP body spine |
| **Total** | **100** | **~75.5** | **~80.5–82.5** | **~+5.0–7.0** | Conservative landing: **78–80** |

---

## 4. PI-A through PI-E Impact

| Pattern | Impact |
|---|---|
| PI-A (multi-stage TRACE pipeline) | W4-C: rehydration ensures runs survive process restart; W4-E: spine consistency across intake→finalizer path |
| PI-B (PI-A + reflect_and_infer) | W4-A: RunPostmortem + EvolveEngine wired for post-run proposal; reflect path gets full spine |
| PI-C (PI-B + Human Gate) | HumanGateRequest spine unchanged (W3-A); W4-E: GateStore exec_ctx already in Wave 10.3 pilot |
| PI-D (PI-B + subrun dispatch) | W4-E: TeamRunRegistry exec_ctx; TeamRun spine complete; W4-D: cross-tenant tested for team routes |
| PI-E (PI-C + PI-D) | Full spine consistency across 8 durable writers proven in `test_intake_to_finalizer_spine_extended.py` |

---

## 5. Platform Gap Status (P-1 through P-7)

| Gap | Status |
|---|---|
| P-1 Provenance (project_id, source_reference) | **CLOSED** — Wave 9 CO-2/4; W4-E adds RunResult.to_dict() body spine |
| P-2 Reasoning trace storage | **PARTIAL** — schema + write hook (Wave 9 TE-5); full side-channel deferred Wave 10.5 |
| P-3 Cross-Run Project aggregation | **CLOSED** — Wave 9 CO-3/4; W4-E TaskContract body spine adds explicit project scoping |
| P-4 Dynamic re-planning API | **NOT STARTED** — Phase 3 |
| P-5 Confidence scoring | **CLOSED** — Wave 9 CO-6 + TE-1/2 |
| P-6 Knowledge Graph inference | **ADDRESSED** — W4-F adds SqliteKnowledgeGraphBackend (research/prod-ready); factory wired; default flip Wave 10.5 |
| P-7 Feedback integration | **PARTIAL** — CalibrationSignal ingest (Wave 8); W4-A EvolveEngine wires ExperimentStore for proposals |

---

## 6. Deferred Items

| Item | Deferred to | Reason |
|---|---|---|
| Class M — agent_kernel boundary spine audit | Wave 11 | Design choice; StoredEvent boundary tested in W4-E |
| Class S — Self-evolution gated update + rollback | Wave 11 | Human-approval design needed; W4-A ExperimentStore plants the seed |
| Remaining 6 RunExecutionContext writers (RunStore, TeamEventStore, FeedbackStore, op_store, gate_api, RateLimiter) | Wave 10.5 | Lower-criticality writers; 5 highest-value covered |
| RunResult/TaskContract **breaking** body spine | Wave 10.5+ | W10.4 ships back-compat enrichment; breaking requires downstream coordination |
| KG SQLite as research/prod default | Wave 10.5 | Backend shipped in W10.4; default flip after one wave of in-flight observation |
| 25-route Skills/Knowledge/MCP/Plugins/Tools registry tenant scoping | Wave 11 | W4-D closes 5 highest-risk routes; rest is registry-scope refactor |
| Recovery state machine **full** (beyond startup rehydration) | Wave 11 | W4-C closes xfail; full state machine (timeout policies, partial-progress) is Wave 11 scope |

---

## 7. Risk Ledger

| Risk | Mitigation |
|---|---|
| W4-D Rule 6 sweep caller breakage | 11 caller files updated; full pytest 4,515/4,515 (pre-existing failures only) |
| W4-B ExtensionManifest scope creep | Existing manifest types preserved as implementations; no removals in Wave 10.4 |
| W4-E + W4-A integration order | W4-A merged first; W4-E cherry-picks resolved cleanly |
| W4-C recovery double-execute under prod | Re-enqueue gated behind `HI_AGENT_RECOVERY_REENQUEUE=1`; default is warn-only |
| W4-F KG default switch | Default kept as JSON; Sqlite available via `HI_AGENT_KG_BACKEND=sqlite` or posture override |

Rule 7 misattributed sites from Wave 10.3 ledger:
- `ops/readiness.py:42` — file does not exist; dropped
- `capability/policy.py` or-DefaultX — pattern not present; dropped

---

## 8. W4-G Gate Run Instructions (requires user action)

T3 gate is STALE (hot-path files changed in Wave 10.4). To close:

```bash
# Step 1: Verify governance scripts
python -m ruff check .
python scripts/check_rules.py           # OVERALL: PASS
python scripts/check_select_completeness.py  # OK
python scripts/check_doc_consistency.py      # OK
python scripts/check_t3_freshness.py         # will show STALE — expected pre-gate

# Step 2: Run T3 gate (requires VOLCE_API_KEY)
python scripts/rule15_volces_gate.py --output docs/delivery/2026-04-26-f5e3cff-rule15-volces.json

# Step 3: Verify freshness
python scripts/check_t3_freshness.py    # must return OK

# Step 4: Backfill this notice
#   - Set "T3 evidence: docs/delivery/2026-04-26-f5e3cff-rule15-volces.json"
#   - Set "Gate pending: false"

# Step 5: Update Wave 10.3 notice (docs/downstream-responses/2026-04-26-wave10.3-...)
#   - Same T3 evidence (bundled run covers both waves)
```

---

## 9. Strategic Trajectory

| Wave | Target | Status |
|---|---|---|
| Wave 10.4 (this) | 75.5 → 80 | **COMPLETE** (T3 gate pending user action) |
| Wave 10.5 | 80 → 85 | Remaining 6 writers + ConfigContract + breaking body spine + 25-route registry scoping |
| Wave 11 | 85 → 90 | agent_kernel boundary + full recovery state machine + self-evolution gated update |
| Wave 12 | 90 → 95 | Performance + multi-region + observability completion + downstream contract finalization |

After Wave 12: hi-agent enters L3 across all 12 dimensions; downstream system development unblocked.
