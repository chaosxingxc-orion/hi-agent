# Trajectory Spec

This document defines trajectory contracts, state machine constraints, and optimizer interfaces.

## Scope
- Node contracts and DAG invariants.
- Stage graph validation.
- Dead-end detection and recovery hooks.

## Current Status
Implemented in `hi_agent/trajectory/*` with tests under `tests/integration`.
