"""agent-server northbound CLI subpackage (W24 I-E).

The ``main`` entry point lives in :mod:`agent_server.cli.main`. We keep
this package's namespace intentionally light so ``python -m
agent_server.cli.main`` does not double-import the module.
"""
