"""Skill runtime package for host-aware skill execution abstractions.

DTOs are defined in ``agent_kernel.skills.contracts``.
Strategy protocols and factory implementations now live in ``hi_agent.skills``;
they are re-exported from ``agent_kernel.skills.contracts`` for backward compatibility.
"""
